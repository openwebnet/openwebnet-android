<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="deviceCard">
        <item name="android:layout_marginTop">@dimen/deviceCard_marginTop</item>
        <item name="android:layout_marginBottom">@dimen/deviceCard_marginBottom</item>
        <item name="android:layout_marginLeft">@dimen/deviceCard_marginLeft</item>
        <item name="android:layout_marginRight">@dimen/deviceCard_marginRight</item>
        <item name="android:paddingTop">@dimen/deviceCard_paddingTop</item>
        <item name="android:paddingBottom">@dimen/deviceCard_paddingBottom</item>
        <item name="android:paddingLeft">@dimen/deviceCard_paddingLeft</item>
        <item name="android:paddingRight">@dimen/deviceCard_paddingRight</item>
    </style>

</resources>
